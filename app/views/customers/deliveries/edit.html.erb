<h1>配送先編集</h1>

<%= form_for (@delivery) do |f| %>

	<h4>郵便番号(ハイフンなし)</h4>
	<%= f.text_field :postcode %>

	<h4>住所</h4>
	<%= f.text_field :address %>

	<h4>宛名</h4>
	<%= f.text_field :direction %>

	<%= f.submit '編集する' %>
<% end %>